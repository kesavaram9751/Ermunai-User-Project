Options -MultiViews
RewriteEngine On

# Allow extensionless URLs like /admin/admin-dashboard to serve /admin/admin-dashboard.html.
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+?)/?$ $1.html [L]

